//! Engine-layer input sanitization.
//!
//! Stub: Task 5 owns the bodies. Scope note that later tasks must preserve — sanitization
//! is engine-layer only: a non-string wire value is `invalid_request` at the protocol
//! layer and never reaches here.
