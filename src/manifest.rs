//! Embedded model manifest: model id → sha256, size, source URL, and serving knobs.
//!
//! Stub: Task 3 owns the pinned entries and cache-path resolution. The manifest is
//! embedded in the binary and versioned with it — Miller never computes a model path.
