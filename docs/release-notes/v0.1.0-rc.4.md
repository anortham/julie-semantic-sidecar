# v0.1.0-rc.4

RC4 is the current prerelease of the Rust semantic sidecar. It was published from commit
`7cab71190b5d5b4424747ffff6eafcb49a94aec0` with four portable archives and matching SHA-256
sidecars.

## Changes

- Adds the `apple-x64-metal-portable` package profile for `x86_64-apple-darwin`.
- Configures GitHub's `macos-15-intel` runner to build and artifact-validate the Apple x64 profile.
- Keeps Metal built into both Apple executables with truthful ready-CPU fallback.
- Strengthens package-profile, workflow-matrix, backend-identity, throughput, and concurrent-process
  evidence contracts.
- Builds packages from a deterministic Cargo-vendored dependency tree and applies a strict,
  content-identity-bound native patch that replaces llama.cpp's three undefined Vulkan
  division-by-zero infinity expressions with bit-exact IEEE-754 values before shader compilation.
- Handles dangling native SONAME links in clean Linux builds without relying on cached target state.
- Keeps `bge-small-en-v1.5-f32` as the standalone default and preserves the frozen
  `julie.embedding.sidecar` v1 wire protocol.

## Released archives

| Profile | SHA-256 |
|---|---|
| `apple-arm64-metal-portable` | `8b6169894c4f72f78c64dcc1eb6a0ec98f022b413bbe16d05bd2a30781620d54` |
| `apple-x64-metal-portable` | `d48294dde655f1878caa7a716db2a4aefdeb0df7816be8525c20073d65e88552` |
| `linux-x64-vulkan-portable` | `10dfd99d0a0847813482145b4f78fc7601a77a4a136a6cb4e72dd729945e38d1` |
| `windows-x64-vulkan-portable` | `3ddcbb981293eef8f3b4446c74cc94ab611113256d6654dd65397962d0f0dce3` |

Runs `30104080099` and `30104924982` built these archives independently from the same commit.
Every archive was byte-identical across the two runs. The second run passed the exact
checksum-bound Windows gate.

## Evidence boundary

- The exact public RC4 Apple arm64 archive has physical Apple Silicon CPU and Metal evidence,
  including CPU/Metal conformance, fallback, throughput, and three-process concurrency.
- Apple x64 is a package candidate. A hosted Intel runner proves compilation, packaging, manifest
  validation, and unpacked artifact behavior; it is not physical Intel-Mac support evidence.
- Linux Vulkan and Windows Vulkan are released package candidates with hosted artifact validation.
  CUDA is not included in the RC4 release. These notes make no
  physical-hardware support claim for those lanes.
- Apple x64 requires exact-archive golden-vector conformance, Metal selection, CPU fallback,
  concurrency, determinism, and performance proof on a physical Intel Mac before support promotion.

## Evidence

See [`2026-07-24-rc4-portable-package-proof.md`](../findings/2026-07-24-rc4-portable-package-proof.md)
for the two-run reproducibility record, public-asset verification, and Apple arm64 physical Metal
proof. Consumer pin updates remain independently verified in each consumer repository.
