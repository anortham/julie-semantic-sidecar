# v0.1.0-rc.5

RC5 was the final prerelease. It was published from commit
`13fff87bcaa9cc93feac465141756f4fc36183f5` with four portable archives and matching SHA-256
sidecars.

## Changes

- Adds the user-local `broker` process so concurrent Miller sessions with the same frozen
  broker/protocol/model identity share one loaded embedding model over local IPC.
- Keeps the spawning client as the owner through an inherited stdin lease; owner loss shuts down
  the broker without a PID file, process registry, detached daemon, or persisted ownership state.
- Serializes accelerated inference, bounds CPU inference, and exposes health facts for the
  resolved backend and accelerator state.
- Uses one user-global accelerator lease across model identities. Non-holders start on CPU, and
  proven accelerator resource exhaustion demotes the broker to CPU instead of repeatedly failing
  or overcommitting device memory.
- Restricts Unix broker directories and sockets to the current user and uses current-user,
  local-only named-pipe security on Windows.
- Makes the pinned `diag_f16` and `tri_f16` Vulkan zero stores type-explicit before shader
  compilation and records the resulting `vulkan-repro-v4` source identity.
- Parses the generated SPIR-V during every Vulkan package build and rejects missing, duplicate, or
  nonzero constant-derived half-float stores before an archive can be produced.
- Preserves the frozen `julie.embedding.sidecar` v1 request protocol and standalone stdio
  `serve` behavior.

## Released archives

| Profile | SHA-256 |
|---|---|
| `apple-arm64-metal-portable` | `4c62e729124ba30640a0b3a8c0f8a4d9f5b8cc4e02a6de640b5baa9039ff2ddc` |
| `apple-x64-metal-portable` | `959ab0e1869f0eeb68f237f1ca1266f0440f33a1b42ebbfe370d2e3fb8be8a6e` |
| `linux-x64-vulkan-portable` | `a2f0bcd0135cc056465d12353572462f877e9ae7ca5a988a0012de1038a4a36f` |
| `windows-x64-vulkan-portable` | `47f9b1bcc149c781d6d95d74e3e0207142d3f587210872758e5b208fef3b091a` |

Runs `30360072357` and `30360964021` independently built byte-identical archives in all four lanes.
Run `30360964021` passed the checksum-bound protected workflow. Both runs' Linux and Windows
package logs recorded successful generated-SPIR-V zero-store verification.

All four public assets were downloaded again, matched their checksum sidecars and retained run-2
bytes, and passed unpacked manifest verification. Their common native patch identity is
`llama-cpp-sys-2-0.1.151:vulkan-repro-v4:4189b1a37fb3e7eece8d3b3785e62c0c7221b3132b9d9cf995e4320805fb2aca`.

## Evidence boundary

The freshly downloaded Apple arm64 asset passed artifact validation and a direct broker lifecycle
smoke: bind, health over the user-local Unix socket, owner-stdin shutdown, stdout purity, and socket
cleanup. This is package evidence, not physical-hardware support evidence.

Every RC5 archive has a new checksum and inherits no RC4 hardware proof. Apple arm64, Apple x64,
Linux Vulkan, and Windows Vulkan remain package candidates until these exact public archives pass
their applicable real-device gates. CUDA is not included.

See
[`2026-07-28-rc5-shared-broker-release-proof.md`](../findings/2026-07-28-rc5-shared-broker-release-proof.md)
for the complete publication record and
[`2026-07-28-vulkan-zero-constant-reproducibility-incident.md`](../findings/2026-07-28-vulkan-zero-constant-reproducibility-incident.md)
for the incident that established the generated-bytecode gate.
