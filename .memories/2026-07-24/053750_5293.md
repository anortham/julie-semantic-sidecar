---
id: checkpoint_52932add
timestamp: 2026-07-24T05:37:50.535Z
tags:
  - miller-takeover
  - phase-9
  - macos-x64
  - packaging
  - claude-review
  - test-hardening
git:
  branch: codex/miller-takeover-macos-x64
  commit: 24ce625
  files:
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
    - README.md
    - docs/rc-promotion-gate.md
    - scripts/package.ps1
    - scripts/package.sh
    - src/package_manifest.rs
    - tests/conformance.rs
    - tests/package_manifest_tests.rs
summary: WHAT
type: checkpoint
context: "A fresh Claude review accepted the macOS x64 packaging implementation
  but identified two low-severity test weaknesses: a trivial negative
  profile-name guard and an unstructured CI runner assertion."
impact: Closes both accepted Claude review findings without changing runtime or
  packaging behavior.
evidence:
  - Focused package inventory test passed after compile-RED for absent
    structural extractor
  - Focused CI artifact-validation lane test passed after compile-RED for absent
    structural extractor
  - "cargo test: 212 passed, 25 ignored, 0 failed"
  - cargo clippy --all-targets -- -D warnings passed
  - cargo fmt --check passed
  - actionlint passed for ci.yml and release.yml
symbols:
  - bash_declared_profiles
  - powershell_declared_profiles
  - artifact_validation_lane
next: Lead agent should review the uncommitted worktree and include both
  Goldfish checkpoint files when committing after approval.
confidence: 5
---

## WHAT

Strengthened the macOS x64 package drift contracts after fresh Claude review.

## WHY

The first package test only prohibited selected name substrings, and the CI test checked the x64 profile and Intel runner independently. Both could miss realistic matrix drift.

## HOW

- Parse the Bash profile case and PowerShell `ValidateSet`, then compare both to the exact six-profile candidate inventory.
- Inject an obsolete `macos-x64` CPU profile in mutation probes and prove each inventory assertion detects it.
- Extract the named lane specifically from the CI `artifact-validation` job and compare the entire contiguous Apple x64 entry to the exact `macos-15-intel`/Metal/Bash mapping.
- Mutate only that lane's runner and prove the structural assertion detects it.

## IMPACT

The package and CI contracts now reject unexpected profile additions and profile-to-runner decoupling instead of relying on independent substring presence.
